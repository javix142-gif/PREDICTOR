# Sueño Bebé

Carpeta reservada para el proyecto Flutter `sueno_bebe_flutter_1.0.0` y su compilación mediante GitHub Actions.

El código fuente debe quedar dentro de esta carpeta. El workflow de compilación debe ubicarse en `/.github/workflows/` en la raíz del repositorio y ejecutar Flutter usando `working-directory: sueno-bebe`.
